import { SearchWidget } from "../../components/searchWidget";

const HomeScreen = () => {
  return (
    <main className="home-screen">
      <SearchWidget />
    </main>
  );
};

export default HomeScreen;
